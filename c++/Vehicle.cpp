#include "Vehicle.h"

Vehicle::Vehicle(int vehicleId, string vehicleNumber, int capacity)
{
    this->vehicleId = vehicleId;
    this->vehicleNumber = vehicleNumber;
    this->capacity = capacity;
}